<?xml version="1.0" encoding="UTF-8"?>

<launch>
    <arg name="robot" value="maggie" />

    <!-- Run the map server -->
    <node name="map_server" pkg="map_server" type="map_server" args="$(find uc3m_maps)/maps/map_1.3.C12_2014-12.yaml">
        <param name="frame_id" value="/map" />
    </node>

    <!-- do not use a simulated time but Linux wall time -->
    <param name="/use_sim_time" value="false" />

    <!-- navigation (real) -->
    <include file="$(find maggie_navigation_config)/launch/real/navigation_real.launch">
        <arg name="robot" value="$(arg robot)" />
    </include>
</launch>
